runtime:
  cache_dir: ./.cache/antora

site:
  title: "Red Hat Workshop - GitOps & Blue/Green & Promotion between environments (Dev Mode)"
  url: http://localhost:3000/rhs-build-course/index.html
  start_page: redhat-workshop-gitops-bluegreen-environments::index.adoc

content:
  sources:
    - url: .
      branches: HEAD
      start_path: documentation

asciidoc:
  # attributes:
  #   title: "Workshop: GitOps & Blue/Green deployments(Dev Mode)"
  extensions:
    - ./lib/tab-block.js
    - ./lib/remote-include-processor.js

ui:
  bundle:
    url: https://github.com/redhat-developer-demos/rhd-tutorial-ui/releases/download/v0.1.9/ui-bundle.zip
    snapshot: true
  supplemental_files: ./supplemental-ui
output:
  dir: ./gh-pages
